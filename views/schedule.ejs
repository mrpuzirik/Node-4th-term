<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <title>Розклад змагань</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
<h1>Розклад спортивних змагань</h1>

<form action="/search" method="get">
  <input type="text" name="team" placeholder="Пошук за командою">
  <button type="submit">Шукати</button>
</form>

<table>
  <tr>
    <th>Дата</th>
    <th>Учасники</th>
    <th>Результат</th>
  </tr>
  <% games.forEach(game => { %>
    <tr>
      <td><%= game.date %></td>
      <td><%= game.team1 %> vs <%= game.team2 %></td>
      <td><%= game.score || 'Немає результату' %></td>
    </tr>
  <% }) %>
</table>

<a href="/admin">Перейти в адмін-панель</a>

</body>
</html>
