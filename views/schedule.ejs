<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <title>Розклад змагань</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <h1>Розклад спортивних змагань</h1>
  <form action="/search" method="get">
    <input type="text" name="team" placeholder="Пошук за командою">
    <button type="submit">Шукати</button>
  </form>
  <table>
    <tr>
      <th>Дата</th>
      <th>Учасники</th>
      <th>Результат</th>
    </tr>
    <% games.forEach(game => { %>
      <tr>
        <td><%= game.date %></td>
        <td><%= game.participants.join(' vs ') %></td>
        <td><%= game.score %></td>
      </tr>
    <% }) %>
  </table>
  <h2>Додати гру (Адмін)</h2>
  <form action="/admin/game" method="post">
    <input type="date" name="date" required>
    <input type="text" name="team1" placeholder="Команда 1" required>
    <input type="text" name="team2" placeholder="Команда 2" required>
    <button type="submit">Додати</button>
  </form>
  <h2>Ввести результат (Адмін)</h2>
  <form action="/admin/result" method="post">
    <input type="number" name="gameId" placeholder="ID гри" required>
    <input type="text" name="score" placeholder="Рахунок (наприклад, 2:1)" required>
    <button type="submit">Зберегти</button>
  </form>
</body>
</html>